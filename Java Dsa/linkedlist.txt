 linked list stores the element with the address of the next element
 types:singly linked list
       doubly linked list
       circular linked list
        
 class Node{
    int data;
    Node next;
    Node(int d){
        data = d;
        next = null;
    }
 }
 Node(int data){
    this.data = data;
    this.next = next;
 }

 insertion in the ending 
 if(head==null){
    head=newNode:
    return;
 }
 Node temp=head
 while(temp.next!=null){
    temp =temp.next;
    i=i+1
 } 
 temp next=newNode



 traverse
 ---------
 New temp=head;
 while(temp!=null);
 sout(temp.data)
 

how to create node at begining position and end and traversal
delete at begining ,at position and end
//searching
boolean search(int value){
   Node temp=head;
   while(temp!=null){
      if(temp.data==value){
         return True;
      }
      temp=temp.next;
   }
   return false;
}

----------------------------------------------------------
cycle detection- 1,2,3,4,5,6,n,3,4,5,6,n,3,4,5,6,n......
floyd linked list -detects the cycle detection
floyds approach--returns the value or the element in less operation
   
   boolean cycle(){
      Node slow=head;
      Node fast=head;
      while(fast!=null && fast.next!=null){
         slow=slow.next;// increase by 1
         fast=fast.next.next;// increase by 2
         if (slow==fast){
            return true;
         }
      }
      return false;
   }
 
 3 functions insert,delete , and one traverse and cycle and reverse..learnt
 ----------------------------------------------------------
                DOUBLY LINKED LIST 
      NULL->10->20->30->40->NULL
          <-  <-  <-  <-

void insertatbegining(int data){
   node newNode=ne Node(data);
   newNode.next=head;
   if(head!=null){
      head.prev=newnode;
   }
   head=newNode;
}
