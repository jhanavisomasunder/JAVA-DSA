import java.util.*;
public class bipartile {
    static boolean isBipartile(ArrayList<ArrayList<Integer>> graph int n){
        // 0=no color
        //1-red
        //2=blue
        int []color=new int[n];
        // check for each node
        for(int start=0;start<n;start++){
            //if already coloured,skip
            if(color[start]!=0){
                continue;
            }
            color[start]=1;
            Queue<Integer> q=new LinkedList<>();
            q.add(start);

            while(!q.isEmpty()){'
               int node=q.poll();
               System.out

            
            '}
        }
    }
}
